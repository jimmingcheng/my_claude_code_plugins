{
  "skills": {
    "ping-me": {
      "description": "Generate a TTS notification with contextual message",
      "command": "node ${CLAUDE_PLUGIN_ROOT}/plugins/ping-me/dist/ping-me.js",
      "timeout": 15,
      "parameters": {
        "message": {
          "type": "string",
          "required": false,
          "description": "Custom message to speak via TTS"
        },
        "reason": {
          "type": "string",
          "required": false,
          "description": "Context or reason for the notification to help generate appropriate message"
        }
      }
    }
  }
}